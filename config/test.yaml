type: co+ss
use_channels: [1,2,3]
targets: [1080Lines, 1400Ripples, Air_Compressor, Blip,
          Chirp, Extremely_Loud, Helix, Koi_Fish, Light_Modulation,
          Low_Frequency_Burst, Low_Frequency_Lines, No_Glitch,
          Power_Line, Repeating_Blips, Scattered_Light, Scratchy,
          Tomte, Violin_Mode, Whistle, Other]
use_other: True
num_classes: 20
train_size: 0.8
batch_size: 128
num_workers: 4
train_steps: 100

labeled_size: 0.1
use_apex: True
opt_level: O1
num_epochs: 10000
weights: [1., 50., 1.]
eval_step: 5
metrics_fn: [latent_space_metrics, multi_class_metrics]
dataset_root: /Users/gen/workspace/github/GravitySpy/processed/dataset_small.h5

wandb:
  project: kagra-deep
  name: test
  tags: None
  group: test


train:
  batch_size: 128
  num_workers: 4
  pin_memory: True
  drop_last: True

eval:
  batch_size: 128
  num_workers: 4
  pin_memory: True
  drop_last: False
